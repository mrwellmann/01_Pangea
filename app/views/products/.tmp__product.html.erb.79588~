<tr class="<%= cycle('list-line-odd', 'list-line-even') %>">
	<td>
		<%= image_tag product.image_url, :class => 'list-image' %>
	</td>

	<td class="list-description">
		<dl>
			<dt><%=h product.product_name%></dt>
			<dd><%=h truncate(product.description.gsub(/<.*?>/,''),:length =>50)%></dd>
		</dl>
	</td>
	
	<td class="list-price_exp">
		<dl>
			<dt><%=h product.price%> €</dt>
			<dd><%=h product.expirience_points%> Exp</dd>
			<%if product.continent!=nil%>
				<dd><%=h product.continent.continent_name%></dd>
			<%end%>		
		</dl>
	</td>
	
	<td class="list-actions">
		<%= link_to 'Show', product %><br/>  
		<% if admin_signed_in? %>                                             
			<%= link_to 'Edit', edit_product_path(product) %><br/>                            
			<%= link_to 'Delete', product, :confirm => 'Do you realy want to delete this shop item?', :method => :delete %>	
		<% end %>				
	</td>
</tr>
